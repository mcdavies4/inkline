export const metadata = { title: "Checkout cancelled — Inkline", robots: { index: false } };

export default function Cancelled() {
  return (
    <main className="wrap">
      <div className="card center">
        <h1>Checkout cancelled</h1>
        <p className="mute">
          No charge was made. Whenever you're ready, reply BILLING in WhatsApp to subscribe and
          unlock unlimited documents.
        </p>
      </div>
    </main>
  );
}
